#Need a Menu for the Files... Will this work?
from printr import *
import pprint
#can I make a server wit python or do I need Django... Ima use Go instead

pp = pprint
o_p_p = pprint.PrettyPrint(compact = True)

class FileList:

    def __init__(self,fil):
        
        self.fil = file

    def pend(self, files):

        self.files.append(file)

    def Sto(self, fill):
        
        fill = []

        if FileList.pend() = +1:

            self.file.append(fills)
    
    
    def menu(self,navBar):#I need a list? User input.. so dictorary inside of an array for rn

        self.navBar = navbar

        navBar = {"(1)":"Folders","(2)":"Settings"}#Gotta remember to add a .addpend method to the Kern not for this Navbar but probably the next
        pp.qprint(navBar)

        qprint("<Input>")

        Barput = input()

        if Barput.islower(navBar.values):
            print(fill.keys)

        else:
            Barput.islower(navbar.key)
            pass # bc I dont have a file for that yet
            #this is a decent starting point
            

        
